# 02 — Gradio 离线界面重构与 asr1.py 彻底清理

**What to build:**
重构 [app_control.py](file:///E:/project/funclip-pro/app_control.py)，使其不再拉起 `subprocess` 命令行弹窗调用 `funclip/asr1.py`，而是直接在进程内导入并调用核心的 `OfflinePipeline`，完成离线 Gradio 界面与算法包的直接归口。并在重构完成后，彻底删除 `asr1.py` 历史包袱。

**Blocked by:**
01 — 物理清理立即可删的历史冗余文件

**Status:** ready-for-agent

- [ ] 修改 [app_control.py](file:///E:/project/funclip-pro/app_control.py)，导入 `from funclip_pro.pipeline import OfflinePipeline` 和 `load_config`。
- [ ] 改造 `run_offline_asr()` 函数：
  * 废除 `subprocess.Popen` 调用 `funclip/asr1.py` 的逻辑（该逻辑原本通过写临时 json 文件传递参数）。
  * 改为在 Python 内部直接实例化并调用 `OfflinePipeline.run()`（通过方法参数直接传递 `audio_input`、`sd_switch` 等参数，不进行额外的进程开销和文件传递）。
- [ ] 在 `run_offline_asr()` 内，保留 Whisper 引擎等目前尚未被包化重构的逻辑分支（如有）。
- [ ] 界面联调测试：双击运行 `一键启动_集成控制台.bat`，测试离线 ASR、分人聚类与 SRT 字幕导出是否一切正常。
- [ ] 确认运行正常后，**彻底物理删除** `funclip/asr1.py` (119KB 冗余单文件) 以及 `funclip/launch.py` 及其依赖链。
- [ ] 运行 Pytest 全量常规门禁，确保没有任何依赖缺失崩溃。
