# 04 — core.asr：下沉 ASR 推理封装

**What to build:** 将 ASR 推理封装下沉到 `src/funclip_pro/core/asr.py`：`SenseVoiceSmall`(ONNX)、`PyTorchSenseVoice`(torch_engine)、`SherpaSenseVoice`(sherpa_engine)、`CharTokenizer`(原 `DefaultTokenizer` 内类)、`load_models`、`_select_engine`、`_decode`、`_post_punc`、`_clean`、`_cheap_trim`、`_use_vad`、`_merge_vad_segments`。决策：根 `asr_service.py` 的无 diarization `_run_inference` 按 spec "统一转写流水线" 并入，由 `OfflinePipeline` 统一调度；本模块只暴露引擎与解码工具，不负责四元组编排（四元组在 pipeline 层）。

**Blocked by:** 01 (包骨架搭建)

**Status:** ready-for-agent

- [ ] 三引擎均可 `from funclip_pro.core.asr import SenseVoiceSmall, PyTorchSenseVoice, SherpaSenseVoice` 独立导入
- [ ] ONNX 推理等价（算法逻辑不变）
- [ ] 路径零硬编码

**Module interface contract:**
```python
class SenseVoiceSmall(SenseVoiceSmallONNX):
    def __init__(self, model_dir, batch_size=1, quantize=True, device_id="-1", intra_op_num_threads=4, **kwargs): ...
    def infer(self, feats, feats_len, language, textnorm): ...
    def load_data(self, wav_content, fs=None): ...
    def __call__(self, wav_content, language=[0], textnorm=[15], tokenizer=None, **kwargs): ...

class PyTorchSenseVoice:
    def __init__(self, model_dir, device="cpu"): ...
    def __call__(self, waveforms): ...

class SherpaSenseVoice:
    def __init__(self, ...): ...
    def __call__(self, wav_content): ...

def load_models(): ...
def _decode(engine_key, waveforms): ...
def _post_punc(raw_text: str) -> str: ...
def _clean(t): ...
def _cheap_trim(audio_path, top_db=SHORT_TRIM_TOP_DB, pad_ms=TRIM_PAD_MS): ...
def _use_vad(vad_strategy, duration_ms): ...
def _merge_vad_segments(segments, max_gap_ms=300, max_duration_ms=8000): ...
```

**Notes:**
- `CharTokenizer` = 原 `asr_onnx_service.py` 内 `DefaultTokenizer`，迁移时重命名为公开 `CharTokenizer` 并保留 tokens2text。
- 算法逻辑与重构前完全等价，不优化 45454 交替 / 字级时间戳对齐（Out of Scope）。
- 最高指导：`.superpowers/spec/2026-07-14-refactor-p0-p1-spec.md` L49。
